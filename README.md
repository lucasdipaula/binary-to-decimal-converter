# Binary To Decimal Converter

This is a simple ReactJS project.

It is a binary to decimal number converter.

## To run

- Clone the repository
- Install dependencies:

    ```yarn install or npm install```

- Start the project:

    ``` yarn start or npm start```
